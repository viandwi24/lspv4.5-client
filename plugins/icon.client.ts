// import Unicon from './../components/Unicon.vue'
// import { uniSun, uniMoon, uniLayerGroupMonochrome, uniCarWash  } from "vue-unicons/dist/icons";
// import VueFeather from 'vue-feather'


export default defineNuxtPlugin((nuxtApp) => {
  // nuxtApp.vueApp.config.compilerOptions.isCustomElement = (tag) => true
  // nuxtApp.vueApp.component('VueFeather', VueFeather)
  // Unicon.add([uniLayerGroupMonochrome, uniCarWash])
  // nuxtApp.vueApp.use(Unicon as any)
  // (Unicon as any).add([uniLayerGroupMonochrome, uniCarWash, uniSun, uniMoon])
})